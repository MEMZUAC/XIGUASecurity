<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="XIGUASecurity.UI.Dialogs.BackupProgressDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XIGUASecurity.UI.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="正在备份文件"
    PrimaryButtonText=""
    CloseButtonText=""
    IsPrimaryButtonEnabled="False"
    IsSecondaryButtonEnabled="False"
    CornerRadius="8">

    <Grid>
        <StackPanel Spacing="20" Margin="24">
            <TextBlock x:Name="StatusText" Text="正在准备备份..." HorizontalAlignment="Center" FontSize="16"/>
            <ProgressBar x:Name="BackupProgressBar" IsIndeterminate="True" Width="300" Height="10"/>
            <TextBlock x:Name="ProgressText" Text="0/0 文件已备份" HorizontalAlignment="Center" FontSize="14"/>
            <TextBlock Text="备份过程中请不要操作主程序窗口" HorizontalAlignment="Center" FontSize="12" Foreground="Gray"/>
        </StackPanel>
    </Grid>
</ContentDialog>